apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: week1-app
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: week1-app
  policy:
    semver:
      range: ">=1.0.0"
